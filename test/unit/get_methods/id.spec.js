/* global describe it before */

import { expect, assert } from 'chai'

describe('get methods', function () {

  describe('getId', function () {

    before(() => {
      const getId = require('../../domali')
    })

    it('should call getElementById', function () {
      expect(true).to.be.true
    })

  })

})
